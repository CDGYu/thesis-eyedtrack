# HANDOFF 2026-07-16 — Threshold Calibration (SDD execution in progress)

**Read in this order:** this file → `.superpowers/sdd/progress.md` (ledger, gitignored but on disk) → ADR-0003 → plan file for the current task.

| Artifact | Path |
|---|---|
| Spec | `docs/superpowers/specs/2026-07-16-threshold-calibration-design.md` |
| Plan (source of truth, 9 tasks) | `docs/superpowers/plans/2026-07-16-threshold-calibration.md` |
| ADR (all decisions + rationale) | `docs/superpowers/adr/0003-threshold-calibration-decisions.md` |
| Ledger (per-task status + logged minors) | `.superpowers/sdd/progress.md` (section "Threshold Calibration Toolchain") |
| Task briefs / reports / review diffs | `.superpowers/sdd/task-N-brief.md`, `task-N-report.md`, `review-*.diff` |
| Memory | `eyedtrack-threshold-calibration.md` in Claude's project memory dir |

## State at handoff

- **Branch:** `feat/threshold-calibration` (branched from `main` @ `ecf4d38`; main untouched since).
- **Commits:** 85839d4 (T1 calib_common core) → f4c6d9c (T2 config editor) → dd6b16c (T3 preprocessing) → f1d686a (T3 review fix: CLAHE default True) → b297267 (T4 prepare_dataset).
- **Tests:** 22/22 passing via `py -3.14 -m pytest dev/tests -v`.
- **Process:** superpowers:subagent-driven-development. Per task: `scripts/task-brief PLAN N` → implementer subagent (haiku when brief contains full code, sonnet otherwise) → `scripts/review-package BASE HEAD` → reviewer subagent (sonnet) → fix loop → ledger line. Scripts live in the superpowers plugin: `C:\Users\user\.claude\plugins\cache\claude-plugins-official\superpowers\6.1.1\skills\subagent-driven-development\scripts\`.

## Exactly where execution stopped

**Task 4 is implemented and committed (b297267) but its REVIEW has not run.**
Ready-made inputs for the reviewer dispatch:
- Brief: `.superpowers/sdd/task-4-brief.md` · Report: `.superpowers/sdd/task-4-report.md`
- Diff package: `.superpowers/sdd/review-f1d686a..b297267.diff` (BASE f1d686a, HEAD b297267)
- Reviewer focus points: subject-disjointness, determinism (RNG/dict-order), DangerousDriving top-up condition, copy collision handling, `p.parts` lowercase comparison on Windows, rerun idempotence (legacy already archived).

## Remaining work (in order)

1. **Task 4 review** (dispatch reviewer with inputs above; fix loop if findings; ledger line).
2. **Task 5:** `build_fixture.py` + 36-image mini_dataset (ONLY detection-verified images; run builder BEFORE any real prepare run — it reads current `dataset/test/`) + `annotate_dataset.py` (mp pool, resume, `--workers 0` inline mode). Implementer: sonnet.
3. **Task 6:** `spotcheck_labels.py` HTML contact sheet (haiku).
4. **Task 7:** `calibrate_thresholds.py` sweeps + figures + thresholds.json + config edit (sonnet).
5. **Task 8:** `evaluate_dataset.py` + sample_figures + e2e smoke test (~60 s, dlib on fixture) (sonnet).
6. **Task 9:** `dev/README.md` operator runbook (haiku).
7. **Final whole-branch review:** `scripts/review-package $(git merge-base main HEAD) HEAD`, dispatch on the most capable model with the ledger's logged-minors list for triage; ONE fix subagent for all findings.
8. **superpowers:finishing-a-development-branch** — merge to main per user preference.
9. **Post-merge (operator, human-in-loop):** Kaggle downloads → prepare → annotate → spotcheck → `evaluate --tag baseline` → calibrate → `evaluate --tag calibrated` (runbook Task 9 documents this).

## Hard-won constraints (do not rediscover)

- `py -3.14` is the working interpreter (dlib 20.0.1, cv2 4.13.0, sklearn/matplotlib/pytest installed via `dev/requirements-dev.txt`).
- **Never use worktrees here:** `*.dat` is gitignored → `shape_predictor_68_face_landmarks.dat` missing → all dlib tests fail.
- Never run `prepare_dataset.py`/`annotate_dataset.py` against real `dataset/` during implementation — tmp_path/fixtures only; real runs are operator runbook steps.
- Never touch `OptimizedFrameProcessor` in dev scripts (temporal counters + VideoRecorder side effects); `ImprovedFaceAnalyzer` only.
- Config edits: only via `edit_detection_thresholds` (comment-preserving); the `thresholds:` YAML section is dead for EAR/MAR/pose.
- Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## Paste-ready resume prompt

```
Resume the EyedTrack threshold-calibration work on branch feat/threshold-calibration.
Read docs/superpowers/HANDOFF-2026-07-16.md, then .superpowers/sdd/progress.md (ledger),
then docs/superpowers/adr/0003-threshold-calibration-decisions.md. Continue
superpowers:subagent-driven-development exactly where the handoff says: dispatch the
Task 4 reviewer (inputs listed in the handoff), then execute Tasks 5-9 from
docs/superpowers/plans/2026-07-16-threshold-calibration.md, then the final
whole-branch review and finishing-a-development-branch. Tasks 1-3 are complete and
reviewed; do NOT re-implement or re-review them. Do not run prepare/annotate against
the real dataset/ — that is the post-merge operator runbook.
```
